import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "flex-start",
    padding: 20,
  },
  errorText: {
    fontSize: 12,
    color: "red",
    marginBottom: 10,
  },
});
