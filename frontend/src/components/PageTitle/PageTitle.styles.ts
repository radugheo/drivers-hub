import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
  },
  text: {
    marginTop: 10,
    fontSize: 30,
    fontWeight: "bold",
    fontFamily: "Montserrat-Bold",
  },
});
